<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        全局暗色细滚动条：解决默认滚动条在深色主题下不可见的问题。
        设计：6px 窄轨 + 半透明滑道（hover 变亮）+ 无按钮，方向自适应（垂直/水平）。
        应用：默认 {x:Type ScrollBar} 全局（App.xaml 引用本文件后自动生效），
        若个别控件需高亮可基于此模板再调整。
    -->

    <!--  滑道按钮隐藏：垂直上箭头 / 垂直下箭头 / 水平左箭头 / 水平右箭头  -->
    <Style x:Key="ScrollThumbVertical" TargetType="Thumb">
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="ThumbBorder"
                            Background="{StaticResource SecondaryBorderBrush}"
                            CornerRadius="3"
                            Opacity="0.55" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Opacity" Value="0.9" />
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource ThirdlyTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollThumbHorizontal" TargetType="Thumb">
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="ThumbBorder"
                            Background="{StaticResource SecondaryBorderBrush}"
                            CornerRadius="3"
                            Opacity="0.55" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Opacity" Value="0.9" />
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource ThirdlyTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  垂直滚动条  -->
    <ControlTemplate x:Key="ScrollBarVerticalTemplate" TargetType="ScrollBar">
        <Grid Background="Transparent">
            <Track x:Name="PART_Track" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False" />
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False" />
                </Track.IncreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollThumbVertical}" />
                </Track.Thumb>
            </Track>
        </Grid>
    </ControlTemplate>

    <!--  水平滚动条  -->
    <ControlTemplate x:Key="ScrollBarHorizontalTemplate" TargetType="ScrollBar">
        <Grid Background="Transparent">
            <Track x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageLeftCommand" Opacity="0" Focusable="False" />
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageRightCommand" Opacity="0" Focusable="False" />
                </Track.IncreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollThumbHorizontal}" />
                </Track.Thumb>
            </Track>
        </Grid>
    </ControlTemplate>

    <!--  全局默认：所有 ScrollBar 走暗色样式  -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="6" />
                <Setter Property="Template" Value="{StaticResource ScrollBarVerticalTemplate}" />
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="6" />
                <Setter Property="Template" Value="{StaticResource ScrollBarHorizontalTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
