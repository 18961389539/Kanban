namespace MainAPP.Views;

/// <summary>
/// 主页仪表板卡片 5：当前班次良率（折线 + 当前良率 + 班次对比）。
/// 仅 XAML 视图，无代码逻辑。DataContext 由父级 HomeView 传入。
/// </summary>
public partial class HomeQualityRateCard
{
    public HomeQualityRateCard()
    {
        InitializeComponent();
    }
}
