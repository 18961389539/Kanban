<Window
    x:Class="MainAPP.Views.ConfigErrorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:MainAPP.Models"
    Title="{x:Static resources:Strings.K140}"
    Width="540"
    Height="380"
    Background="{StaticResource RegionBrush}"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    PreviewKeyDown="OnPreviewKeyDown"
    mc:Ignorable="d" xmlns:resources="clr-namespace:MainAPP.Resources">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            FontSize="14"
            FontWeight="Medium"
            Foreground="{StaticResource PrimaryTextBrush}"
            Text="{x:Static resources:Strings.K141}" />

        <TextBlock
            Grid.Row="1"
            Margin="0,4,0,8"
            FontSize="12"
            Foreground="{DynamicResource SecondaryTextBrush}"
            Text="{x:Static resources:Strings.K142}" />

        <ListBox
            x:Name="ErrorList"
            Grid.Row="2"
            ItemsSource="{Binding Errors}"
            MouseDoubleClick="OnItemDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:DeviceConfigError}">
                    <TextBlock
                        Margin="4"
                        Foreground="{StaticResource PrimaryTextBrush}"
                        Text="{Binding Message}"
                        TextWrapping="Wrap" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button
            Grid.Row="3"
            Margin="0,12,0,0"
            HorizontalAlignment="Right"
            Width="80"
            Click="Close_Click"
            Content="{x:Static resources:Strings.K143}"
            IsCancel="True"
            IsDefault="True"
            Style="{StaticResource ButtonPrimary}" />
    </Grid>
</Window>
