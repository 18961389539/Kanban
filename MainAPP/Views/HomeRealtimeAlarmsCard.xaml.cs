namespace MainAPP.Views;

/// <summary>
/// 主页仪表板卡片 3：实时故障（卡片式列表 + H/M/L 级别筛选 + 静音按钮 + 空状态）。
/// 仅 XAML 视图，无代码逻辑。DataContext 由父级 HomeView 传入。
/// </summary>
public partial class HomeRealtimeAlarmsCard
{
    public HomeRealtimeAlarmsCard()
    {
        InitializeComponent();
    }
}
