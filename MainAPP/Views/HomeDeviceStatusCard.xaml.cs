namespace MainAPP.Views;

/// <summary>
/// 主页仪表板卡片 1：设备状态（当前状态 + 状态饼图 + 图例）。
/// 仅 XAML 视图，无代码逻辑。DataContext 由父级 HomeView 传入。
/// </summary>
public partial class HomeDeviceStatusCard
{
    public HomeDeviceStatusCard()
    {
        InitializeComponent();
    }
}
