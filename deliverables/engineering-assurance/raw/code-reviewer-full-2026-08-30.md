# Cody 代码审查报告 - Kanban 全解决方案

**日期**：2026-08-30
**审查人**：科迪（Cody）· 代码审查师
**基线**：`deliverables/engineering-assurance/review-full-2026-08-17.md`（2026-08-17 全量审查）

**审查范围**：MainAPP、Kanban.Web、Kanban.Collector、Kanban.Collector.Core、Kanban.Contracts、Kanban.Client、Kanban.Analysis、LicenseIssuer.Wpf、LicenseManager.App、PlcSimulator。
**排除**：`oxyplot/`（第三方）、全部测试项目（MainAPP.Tests / MainAPP.E2E / MainAPP.UIAutomation / PlcSimulator.Tests / MainAPP.Benchmarks）、`bin/`、`obj/`、`.cache`、`*.dll`。

**方法**：
1. **增量复查**：逐条回到上轮报告给出的 `文件:行号`，读当前代码判定「已修复 / 未修复 / 部分修复」，并给出当前代码证据。
2. **模式化搜索挖新缺陷**：空 `catch {}`、`new Timer/DispatcherTimer` 生命周期、`Dictionary<>` 跨线程、`lock` 可变字段、SQL 字符串拼接（`FromSqlRaw`/`ExecuteSqlRaw`/`$"SELECT..."`）、硬编码口令/密钥、同步阻塞异步（`.GetAwaiter().GetResult()`）、`async void`、事件订阅 `+=`/`-=` 配对、`int.Parse` 未保护。
3. **链路跟踪**：对每条候选缺陷，向上追调用方、向下追被调方，确认真实可达后再定级；不可达或无法确认的一律标注「未验证」。

**证据纪律**：本报告中 P0/P1 结论均基于实际读到的代码，给出 `文件:行号`。标注「未验证」的条目为静态推断、未确认运行时可达性，请勿直接当作定论排期。

---

## 一、上轮整改复查

| 上轮编号 | 问题 | 当前状态 | 代码证据 |
|---|---|---|---|
| **P0-1** | 跨页导航事件从未订阅（懒加载双创建路径） | ✅ **已修复** | 新增 `MainWindowViewModel.AttachPageViewModel(object)`（`MainAPP/ViewModels/MainWindowViewModel.cs:489-509`），由 `MainWindow.ActivatePage`（`MainAPP/MainWindow.xaml.cs:95-106`）在 **DI 真实创建路径**上调用；`_attachedPageViewModels`（`MainWindowViewModel.cs:482`）HashSet 做重复订阅去重，5 个 `SubscribeXxx` 全部加 `_attachedPageViewModels.Add(vm)` 守卫（`:514/521/528/539/546`）；`Dispose():565-588` 改为遍历该集合解绑，取代原先恒为 false 的 `IsValueCreated` 死代码。经查 `MainAppPresentationServiceCollectionExtensions.cs:62/71/131` 相关 VM 均为 `AddSingleton`，两条路径解析到同一实例，去重有效。 |
| **P1-1** | `DataSourceAlarmTracker._states` 无锁并发读写 | ❌ **仍未修复** | `Kanban.Collector.Core/Services/DataSourceAlarmTracker.cs:33-36` 仍是 `private readonly Dictionary<string, ValueAlarmState> _states` + `private bool _pendingReconnect`，**全文无 `_lock` 字段**，与同类 `AlarmStateTracker.cs:37`（`private readonly object _lock`）形成鲜明对比。写方：`Observe()` 于 `:97` 执行 `_states[key] = state`（采集轮询线程，经 `PlcScanPipeline.ScanSources`）；读改写方：`RecoverAllOnDisconnect():274` `_states.Clear()`、`ResetAll():292` `_states.Clear()`、`RemoveDevice():299-300`、`RemoveSource():307-308`——后两者由 `PlcDataAcquisitionService.RemoveDeviceState():1271`（UI 线程，设备管理删除设备）与 `ResetDeviceProduction():1242`（`Task.Run` 后台线程）调用。唯一改善是遍历前加了 `.ToList()` 快照（`:266/284/299/307`），只防「枚举中修改」，**不防并发读写**。 |
| **P1-2** | SQLite PRAGMA 拦截器用 `Task.Run` 跨线程执行 | ✅ **已修复** | `Kanban.Collector.Core/Data/DatabaseProvider.cs:523-528`：`ConnectionOpenedAsync` 改为 `cancellationToken.ThrowIfCancellationRequested(); ConnectionOpened(connection, eventData); return Task.CompletedTask;`，`Task.Run` 已移除。 |
| **P2-1** | 会话字段（RunTime/AlarmTime/TotalOkProduction）并发读改写 | ❌ **仍未修复** | 轮询线程写：`PlcDataAcquisitionService.cs:988` `runtime.RunTime += intervalSeconds`、`:990` `AlarmTime +=`、`:797` `rt.TotalOkProduction = v`（`AccumulateOeeTime` 由 `:429` 主循环调用）。清零方：`ResetDeviceProduction():1232-1238` 在 `lock (_resetLock)` 内执行 `GetRuntime(device)?.ResetShift()`。但 `AccumulateOeeTime` / `TryReadProductionCount` **不在 `_resetLock` 内**，`_resetLock`（`:105`）只保护 `ResetShift↔ResetDeviceProduction` 互斥。调用方 `MainAPP/Services/DevicePlcCommandHandler.cs:114` `await Task.Run(() => _dataAcquisitionService.ResetDeviceProduction(device))` 确认清零发生在后台线程。 |
| **P2-2** | 配方校验用通用 `PlcAddressParser` 而非品牌 codec | ❌ **仍未修复** | `Kanban.Collector.Core/Services/RecipeValidator.cs:62` 仍为 `var parse = PlcAddressParser.Parse(item.PlcAddress);`。`PlcAddressParser.cs:74-85` 的正则集合仍只覆盖 D / M / Siemens DB / Modbus HR·IR·C·DI，**无 Omron（W100、D100.05）与 Keyence（DM100、MR100）** 模式。→ 见本报告新发现 **N-1**（同一文件暴露出更严重的溢出问题）。 |
| **P2-3** | `EnsureConnected` 与 `Disconnect` 竞态 | ⚠️ **部分修复** | 改善：`PlcConnectionManager.cs` 已把状态锁 `_stateLock` 与驱动 IO 锁 `_driverOperationLock` 分离（`EnsureConnected():158-179`；`Disconnect():330-349`），状态查询不再被 PLC 超时阻塞。残留：`Disconnect()` 在 `:332` 置 `IsConnected=false` 后，若在途的 `EnsureConnected` 正停在 `lock (_driverOperationLock)` 之后的 `:189 lock(_stateLock){ IsConnected = result.IsSuccess; }`，仍会把 `IsConnected` 置回 `true`（socket 已被 `Disconnect` 关闭）。无代际号/版本号校验。影响：短暂的「状态=已连、socket=已关」，下一次读取失败经 `MarkDisconnected` 自愈。严重度可从 P2 降为 P3。 |
| **P2-4** | `?operator=` 客户端自报无校验 | ❌ **仍未修复** | `Kanban.Collector/Hubs/KanbanAdminHub.cs:186-188`：`var http = Context.GetHttpContext(); var fromQuery = http?.Request.Query["operator"].ToString(); return string.IsNullOrWhiteSpace(fromQuery) ? string.Empty : fromQuery;` —— 无长度上限、无字符白名单、无换行/控制字符过滤。审计归属（`KanbanAdminHub.cs:41/53/58/73/79/91/98/109/114/126/131/142/147/169/175`）完全可被冒名（如 `?operator=admin`）。 |
| **P2-5** | 撤销只在签发机本地标记，客户端从不校验吊销列表 | ✅ **不再适用** | 上轮定位的 `LicenseIssuer.CLI/RevokeCommand.cs` 已不存在：项目已被 `LicenseIssuer.Wpf/` 取代（仅 `App.xaml.cs` / `IssuerStore.cs` / `IssuedRecord.cs` / `MainWindow.xaml.cs` 四个文件）。`IssuerStore.cs` 只有 `Save()`，**无任何吊销/撤销能力**；`LicenseManager.App/Services/LicenseGate.cs` 亦无吊销校验。→ 「撤销后仍可用」的漏洞随之消失，但**系统现在完全没有吊销手段**，属于能力缺失而非缺陷，建议产品侧确认是否需要在 V2 补上（若补，须同时下发吊销列表到客户端）。 |
| **P2-6** | 报警/状态订阅方法缺 `OperationCanceledException` 处理 | ❌ **仍未修复** | `Kanban.Collector/Hubs/KanbanHub.cs:81-87`（`SubscribeAlarmEventsAsync`）与 `:90-96`（`SubscribeStatusEventsAsync`）均无 try/catch；同文件 `:74-77`（`SubscribeSnapshotsAsync`）与 `:153-156`（`SubscribeMetaAsync`）**有** `catch (OperationCanceledException) when (Context.ConnectionAborted.IsCancellationRequested)`。同一文件内四条订阅路径处理不一致，前两条在客户端正常断开时异常沿 Hub 方法上抛。 |
| **P2-7** | 报警中心定时器切页后永不停止 | ❌ **仍未修复** | `MainAPP/Views/AlarmCenterView.xaml.cs:25-30` 仍依赖 `Unloaded += OnUnloaded` 调 `vm.Stop()`。根因未消除：`MainAPP/Controls/NavigationPageHost.cs:68-77` 的 `EnsureViewLoaded()` 有 `if (Page is null \|\| Content is not null) return;`，一旦 `Content` 被赋值就**永不清除**；宿主容器 `MainAPP/MainWindow.xaml:402-418` 是常驻 `ItemsControl`（每个页面一个 `NavigationPageHost`，仅切 `Visibility`）。→ 视图永不从视觉树移除 → `Unloaded` 永不触发 → `_activeTimer`(3s) / `_statsTimer`(60s)（`AlarmCenterViewModel.cs:372-382`）常驻。`AlarmCenterViewModel` **未实现** `INavigationPageLifecycle`（对比 `HomeViewModel.cs:35`、`DataSourceMonitoringViewModel.cs:142`、`RuntimeMonitoringViewModel.cs:49` 均已实现），是三个页面里唯一没跟上修复的。→ 同类问题见新发现 **N-3**（Overview 页更严重）。 |
| **P2-8** | 关窗时无条件 `vm.DeviceManagerViewModel.TryCloseWithDirtyCheck()` | ❌ **仍未修复** | `MainAPP/MainWindow.xaml.cs:246`：`if (DataContext is MainWindowViewModel vm && !vm.DeviceManagerViewModel.TryCloseWithDirtyCheck()) e.Cancel = true;` —— 访问 `DeviceManagerViewModel` 属性即触发 `MainWindowViewModel.cs:49` 的 `_deviceManagerLazy.Value`，强制实例化设备管理 VM 及其整条服务链（HistoryService / EF Core / CSV IO 等），违背懒加载初衷。未加「当前不在设备管理页则跳过」的短路判断（对比 `Navigate()` 在 `MainWindowViewModel.cs:331-333` 已做同样的短路，可照抄）。 |
| **P2-9** | 元数据设备集变更只按 `Count` 判断 | ❌ **仍未修复** | `Kanban.Web/DashboardState.cs:552`：`if (meta.Devices.Count != _workOrdersByDevice.Count)` —— 删 1 台 + 增 1 台（总数不变）走 `else` 增量分支（`:559-567`），被删设备的 `_workOrdersByDevice` 条目永不清理，残留工单长期显示。 |
| **P2-10** | 设备 tombstone 只清 `_snapshots` | ❌ **仍未修复** | `Kanban.Web/DashboardState.cs:474-481`：tombstone 分支只做 `_snapshots.Remove(snapshot.DeviceId)`，`_speedHistoryByDevice`（`:25`）、`_workOrdersByDevice`（`:26`）、`_trackedRunningWorkOrderIdByDevice`（`:544/549`）、`_lastShiftsByDevice`（`:577`）条目全部残留。设备增删频繁时内存单调增长。 |
| **P2-11** | 报警查询带 `AlarmId` 时忽略时间窗 | ❌ **仍未修复** | `Kanban.Collector/Services/HistoryQueryHandler.cs:209-216`：带 `AlarmId` 时走 `_history.GetLatestAlarmEventStrict(request.AlarmId)`（全局最新一条），`from`/`to` 参数完全未参与。查「昨天 + 某报警」会拿到今天的记录。 |
| **P2-12** | 只 `OrderByDescending(Timestamp)` 无稳定次级键 | ⚠️ **部分修复** | 已修：`ProductionHistoryStore.cs:48-55` 分页查询已加 `.ThenBy(log => log.Id)`（注释标注「审查修复 2026-08-13」）。未修：`:66` `QueryLatestProductionLog`、`:141` `GetLatestProductionBeforeStrict` 仍只有 `OrderByDescending(log => log.Timestamp)`，同刻多设备「最新」不确定。（`AlarmHistoryStore` / `StatusTransitionStore` 的同名方法**未验证**是否已同步修复。） |
| **P2-13** | 分页每页重算 `Count()` + `Skip(offset)` 深页全量扫描 | ⚠️ **部分修复（可接受）** | 关键改善：分页已下推为 SQL `Skip/Take`（`ProductionHistoryStore.cs:48-55`）+ `Count()`（`:45`），配合 `HistoryQueryHandler.cs:199-202/226-227` 服务端分页，**消除了「百万级记录全量物化 + 全量 SignalR 传输」**这一主要危害。残留：`Count()` 每页全量扫描、`Skip(offset)` 深页成本仍在，但已被 `HistoryQueryLimits.MaxFetchAllPages=200 × MaxPageSize=500 = 10 万行` 上限（`Kanban.Contracts/HistoryQueryLimits.cs:11/28`）封顶。建议按 P3 处理。 |
| **P2-14** | legacy patch 硬编码「已知新增」列/索引子集 | ⚠️ **部分修复** | 改善：`DatabaseProvider.cs:153-163` 改为「`Migrate()` 之后统一再补一遍 legacy patch」，覆盖「事后手工改坏结构」的既有库；`:158-159` 无待执行迁移时也补一次备份；新增 `PruneOldBackups()`（`:184-208`）只保留最近 3 份 `.pre-migration-*.bak`，防止崩溃重启循环撑满磁盘。残留：① `applyLegacyPatch` 仍是硬编码子集（`:210-235`），将来模型加列若忘记补 `EnsureColumn` 仍会缺列；② `IsSchemaCompatible` 失败的异常文案（`:125-127`）仍不指明缺哪一列。 |
| P3-1 | `_pendingReconnect` 全局单标志 | ❌ 未修复 | `DataSourceAlarmTracker.cs:36`，`:104-109` 重连后仅首个值项消费该标志。 |
| P3-5 | 多个标 Async 的写方法实际同步 IO | ⚠️ 部分修复 | `ConfigSyncHandler.SaveDevicesAsync():59-112` 等方法体仍是同步 IO + `return Task.CompletedTask`，跑在 Hub 调度线程；但已通过 `lock (_deviceConfigLock)`（`:27`）保证互斥，吞吐风险降低。 |
| P3-6 | `ReplaceAll→SaveAll` 两步非原子 | ✅ **已修复** | `ConfigSyncHandler.cs:90-110`：`ReplaceAllAndSave(entities)` 整体包在 `lock (_deviceConfigLock)` 内；`RollbackDevicesAsync():121` 与 `HasDeviceBackupAsync():156` 同样加锁。 |
| P3-7 | PBKDF2 iterations 从存储哈希解析无上界 | ❌ 未修复 | `Kanban.Collector.Core/Services/PasswordHasher.cs:44`：`if (!int.TryParse(parts[1], out var iterations) \|\| iterations <= 0) return false;` —— 只有下界无上界。旧 SHA256 哈希验证通过（`:60-81`）后仍不自动 rehash 升级。 |
| P3-9 | `_consecutiveFailures` 混用 Interlocked 与普通赋值 | ❌ 未修复 | `Kanban.Client/KanbanDataClient.cs:125` 用 `Interlocked.Increment`，而 `:134` 与 `:154` 用普通赋值 `_consecutiveFailures = 0`。 |
| P3-10 | 两处 `async void` | ✅ **已修复** | 全解决方案 `async void` 仅剩 `MainAPP/App.xaml.cs:62` 的 `OnStartup`（WPF 框架签名强制），且 `:88/295/306` 已加全局 try/catch + 优雅退出。上轮的 `RuntimeMonitoringViewModel.cs:279` / `SettingsViewModel.cs:842` 已消除。 |
| P3-11 | 快捷键绕过未保存确认 | ✅ **已修复** | `MainWindowViewModel.cs:759-770`：`SelectPage` 改为转调 `Navigate(page.Key)`，复用同一套权限门禁与未保存检查（`Navigate` 的脏检查见 `:331-337`）。残留（已知并有意豁免，见 `:328-330` 注释）：`RefreshNavigationForCurrentUser():375` 登录/权限变更的强制回退直改 `SelectedIndex`，不经过 `Navigate`。 |
| P3-12 | `new List<T>(first.Total)` 按服务端 Total 预分配 | ❌ 未修复 | `Kanban.Web/Services/HistoryFetch.cs:54`：`var all = new List<T>(first.Total);`，实际最多只拉 `MaxFetchAllPages(200) × MaxPageSize(500) = 10 万` 条。WASM 单线程内存受限，服务端返回异常大 `Total` 时可直接 OOM。 |
| P3-13 | `ReplayRecoveryAsync` 仅在收到新数据时调用 | ❌ 未修复 | `ProductionHistoryWriter.cs:89-123`：循环体 `try` 中 `await ReplayRecoveryAsync(ct)` 只在 `:99 FlushPendingAsync` 之后执行，而 `:96-98` 的 `WaitToReadAsync` 5s 超时走 `catch (TimeoutException)` 分支（`:102-105`）**只调 `FlushPendingAsync`，不调 `ReplayRecoveryAsync`**。设备静止时恢复文件数据回放被无限推迟（仅进程停机时 `:122` 补一次）。 |

**复查小结**：上轮 1 项 P0 已修复（且修法干净，用了 HashSet 去重 + 遍历解绑）；2 项 P1 修复 1 项；14 项 P2 中**完全修复仅 1 项（P3-6 计在此档对比）**，其余 13 项中 3 项部分修复、9 项未动、1 项不再适用。整体趋势是：**新写的代码（SignalR 订阅/分页下推/事件注册去重/输入校验）质量明显提升，但上轮点名的存量并发与生命周期缺陷基本没动**。

---

## 二、新发现缺陷（按严重度排序）

| # | 严重度 | 类别 | 文件:行号 | 问题描述 | 建议修复 | 证据 |
|---|---|---|---|---|---|---|
| **N-1** | 🔴 **P0** | correctness / 健壮性 | `Kanban.Collector.Core/Services/PlcAddressParser.cs:104,111,120,129,136,141,147,154,162,169,176,183`（根因）；受害点 `Kanban.Collector.Core/Data/RecipeStore.cs:116` | **单条越界 PLC 地址可让整个配方库被清空 / 让全厂报警扫描持续失效。** `PlcAddressParser.Parse` 对匹配成功后的数字段一律用 `int.Parse` 解析，`D99999999999999` 这类「格式合法但数值溢出」的地址会抛 `OverflowException`（不是返回 `Invalid`）。三条已验证的受害链路：<br>**A（数据丢失）** `RecipeStore.LoadAll()` 在 `try` 内对每条配方调 `RecipeValidator.Validate(r, Recipes)`（`RecipeStore.cs:116`）→ 抛异常 → 落到 `RecipeStore.cs:134-143` 的 `catch` → **把 recipes.json 整体 `File.Move` 成 `recipes.json.corrupt` 并回退为空配方列表**。一条坏记录 → 全部配方丢失。<br>**B（监控静默失效）** `AlarmStateTracker.ScanAlarms()` 的 `PrepareBatchValues`（`AlarmStateTracker.cs:65` → `:261` `.Select(alarm => adapter.AddressCodec.Parse(alarm.PlcAddress))`）与逐条循环 `:72` 都**没有 per-device / per-alarm 的 try**；三菱等 codec 直接委托 `PlcAddressParser`（`PlcAddressCodecs.cs:44-49`）。一台设备一个坏地址 → 异常冒出 `ScanAlarms` → 被 `PlcDataAcquisitionService.TryScan():618-631` 捕获后**只记 LogError 并返回 false** → **全厂所有设备的报警该轮一条都不扫描，且每 200ms 重复失败**。<br>**C（UI 静默失败）** `MainAPP/ViewModels/RecipeManagerViewModel.cs:401` 的 `RecipeValidator.Validate` 调用位于 `try`（`:409`）**之外**；`SaveRecipeAsync` 是 `async Task` + `[RelayCommand]`，异常被 `AsyncRelayCommand.ExecutionTask` 吞掉 → 用户点保存无任何反馈。<br>注：上轮 P3-3 已记录此 `int.Parse` 问题但定级 P3，实测爆炸半径远大于此，故上调。 | ① `PlcAddressParser.Parse` 内所有 `int.Parse` 改为 `int.TryParse`，失败即 `return PlcAddressParseResult.Invalid(trimmed, "地址数值超出范围")`（这是根因，一处修完 A/B/C 同时收敛）。<br>② `RecipeStore.LoadAll()` 把 `RecipeValidator.Validate` 调用单独包 try，校验抛异常只跳过该条、**不得**进入清空整个库的 catch 分支。<br>③ `AlarmStateTracker` 的 `PrepareBatchValues` 与 `ScanAlarms` 逐条循环加 per-item try/catch，单条地址解析失败只跳过该报警。<br>④ `RecipeManagerViewModel.SaveRecipeAsync` 把 `Validate` 挪进 try，或捕获后 `NotifyWarning`。 | 根因代码（已读）：`PlcAddressParser.cs:134-137` `if (DPattern.IsMatch(trimmed)) { return PlcAddressParseResult.Valid(trimmed, PlcAddressType.DWord, int.Parse(trimmed[1..]), 2, "D"); }`。<br>链路 A：`RecipeStore.cs:103` `try {` → `:116` `var errors = RecipeValidator.Validate(r, Recipes);` → `:134` `catch (Exception ex)` → `:138-142` `File.Move(FilePath, corruptPath)`。<br>链路 B：`AlarmStateTracker.cs:65` `var batchValues = PrepareBatchValues(...)`（无 try）→ `:261`；`:68-72` 逐条 `adapter.AddressCodec.Parse(addr)`（无 try）→ `PlcDataAcquisitionService.cs:438` `_ = TryScan(() => _scanPipeline.ScanAlarms(), ...)` → `:629` `_logger.LogError(ex, "{ScanName} 扫描发生业务异常（不影响 PLC 连接状态）")`。<br>链路 C：`RecipeManagerViewModel.cs:379-380` `[RelayCommand] private async Task SaveRecipeAsync()`，`:401` Validate 在 `:409 try` 之外。 |
| **N-2** | 🟠 **P1** | concurrency / performance | `Kanban.Collector.Core/Services/SharedPlcDriverRouter.cs:91-98`（`WithCurrent`） | **在互斥锁内执行全部阻塞式 PLC IO。** `Connect/Disconnect/ReadInt32/ReadBool/ReadInt32Batch/WriteInt32/...`（`:75-89`）全部经 `WithCurrent` 包一层 `lock (_sync)`。PLC 单次 IO 典型耗时几十 ms，TCP 握手/断线超时可达数秒。后果：① **200ms 采集轮询线程与 UI 手动操作（配方下发、单设备产量清零、报警复位，经 `DevicePlcCommandHandler.cs:75/114/145/176` 的 `Task.Run`）完全串行化**——轮询持锁做一次批量读（上百 ms）时，用户的手动写被阻塞；用户在 `Configure()`（`:52-73`）里换驱动、等锁时，采集线程同样卡住。② 品牌切换路径 `:58-65` 在锁内执行 `_factory.Create` + `_current.Dispose()`，进一步拉长持锁时间。对比：`PlcConnectionManager` 已正确地把状态锁与 IO 锁分离（见 P2-3 复查），此处尚未跟上。 | 把「取当前驱动引用」与「执行 IO」分离：`WithCurrent` 只做 `lock (_sync) { if (_disposed) throw; var driver = _current; }` 取快照，锁外执行 `operation(driver)`。为防止 `Dispose`/品牌切换期间的 use-after-free，改用引用计数或延迟释放（`ReaderWriterLockSlim`：IO 取读锁、品牌切换/Dispose 取写锁），或让 `Dispose` 只标记 `_disposed`、旧驱动延迟到下次切换时才真正释放。 | `SharedPlcDriverRouter.cs:91-98`：<br>`private TResult WithCurrent<TResult>(Func<IPlcDriver, TResult> operation)`<br>`{ lock (_sync) { if (_disposed) throw new ObjectDisposedException(...); return operation(_current); } }`<br>调用面：`Connect()` `:75`、`Disconnect()` `:76`、`ReadInt32Batch()` `:79`、`WriteInt32()` `:86`。<br>跨线程调用方：`DevicePlcCommandHandler.cs:114` `await Task.Run(() => _dataAcquisitionService.ResetDeviceProduction(device))`。 |
| **N-3** | 🟠 **P1** | resource-leak / performance | `MainAPP/Views/OverviewView.xaml.cs:32-57`；根因 `MainAPP/Controls/NavigationPageHost.cs:68-77` + `MainAPP/MainWindow.xaml:402-418` | **概览页 2Hz 四图重绘定时器永不停止，且 OverviewView（含 4 个 OxyPlot PlotView）被 DI 单例 VM 永久持有无法 GC。** 与 P2-7 同根因：所有页面 host 常驻 `ItemsControl`（`MainWindow.xaml:402`），只切 `Visibility`，`NavigationPageHost.EnsureViewLoaded()` 有 `if (Content is not null) return;` 导致视图一旦加载**永不卸载**，`Unloaded` 永不触发。因此 `OverviewView.OnUnloaded():51-57`（`_chartRefreshTimer.Stop()` + `vm.PropertyChanged -= OnViewModelPropertyChanged`）永远不会执行。比报警中心更重：① 定时器是 **500ms（2Hz）** 而非 3s/60s；② 每次 tick 调用 `RefreshChart()`（`:119-125`）对 4 张图做 `view.Model = null; view.Model = model; view.InvalidatePlot(true)` **全量重绘**；③ `OnDataContextChanged():44-47` 订阅的 `OverviewViewModel.PropertyChanged` 不解绑，`OverviewViewModel` 是 `AddSingleton`，整棵视图树（4 个 PlotView + 数据网格）被永久钉住。<br>触发条件极宽：代码注释自述「产线 tick 一次抛 15+ 条 PropertyChanged」（`:19-21`），只要概览页被打开过一次，切到任何页面后重绘都在继续。 | 与 P2-7 合并修复：`AlarmCenterViewModel` / `OverviewViewModel` 实现 `INavigationPageLifecycle`（参考 `DataSourceMonitoringViewModel.cs:309-323` 的 `OnPageEnter/OnPageExit` 模板），把 `Start/Stop` 与事件订阅/解绑搬到生命周期方法；由 `MainWindow.ActivatePage()`（`MainWindow.xaml.cs:101-105`）在切换时驱动。视图侧删除 `Loaded/Unloaded` 依赖。 | `OverviewView.xaml.cs:32-37` `Loaded += OnLoaded; Unloaded += OnUnloaded; ... _chartRefreshTimer = new DispatcherTimer(ChartRefreshInterval /*500ms*/, ...)`；`:51-57` `OnUnloaded` 内含 `vm.PropertyChanged -= OnViewModelPropertyChanged; _chartRefreshTimer.Stop();`（永不执行）；`:102-103` `if (!_chartRefreshTimer.IsEnabled) _chartRefreshTimer.Start();`。<br>`NavigationPageHost.cs:70` `if (Page is null \|\| Content is not null) return;`（Content 永不置回 null，全文无 `Content = null`）。<br>`MainWindow.xaml:402` `<ItemsControl ItemsSource="{Binding Pages, ElementName=RootWindow}">`。<br>对照组：`HomeViewModel.cs:513-525`、`DataSourceMonitoringViewModel.cs:309-323` 已用生命周期接口。 |
| **N-4** | 🟠 **P1** | security | `Kanban.Collector.Core/Services/UserStore.cs:65-87`（降级路径）+ `:28-29`（公开默认口令） | **破坏 `users.json` 即可把整机的账号体系重置为源码公开的默认口令。** `UserStore.Load()` 在 JSON 反序列化/迁移抛异常时（`:74 catch`）走兜底：备份为 `.corrupt` 后 `_users = CreateDefaultUsers(); Save();`（`:85-86`）。而 `CreateDefaultUsers():299-331` 会用**源码中的硬编码常量**重建 `admin/gly`（管理员）、`engineer/gcs`（工程师）、`operator`（免密）。口令常量就在同文件 `UserStore.cs:28-29`（`DefaultAdminPassword = "gly"` / `DefaultEngineerPassword = "gcs"`），注释 `:15` 声称「源码不含明文密码」，与该常量实际存在相矛盾。<br>攻击面：任何能写 `users.json`（进程数据目录，通常为普通用户可写）的本地主体——恶意软件、误操作的备份恢复、磁盘损坏——只要让文件不可解析，下次启动即获得管理员口令 `gly`。管理员账号因此**永远无法靠改密码来自我保护**：改了密码也没用，破坏一次文件就回到 `gly`。<br>附带问题：`operator` 账号 `PasswordHash = string.Empty` 即免密登录（`:145-150`「PasswordHash 为空 = 免密账号，跳过密码验证」），且 `EnsureOperatorAccount():338-354` 在每次加载发现缺失时都会重新补齐。 | ① 默认账号**只在 `users.json` 不存在时**创建（首次安装），解析失败时**不得**重建默认账号——应中止启动并提示人工介入（与 `DatabaseProvider.cs:125-128` 对数据库的处理口径一致：宁可阻断也不能静默重置安全边界）。<br>② 若必须兜底，重建的 admin 应设置为**随机一次性口令 + 强制改密标记**，或标记为 `IsActive=false`。<br>③ 删除 `DefaultAdminPassword` / `DefaultEngineerPassword` 常量（或改为首次安装时随机生成并打印到安装日志）。<br>④ 复核 `operator` 免密账号是否为产品必需；若必需，至少在审计日志中显著标记其操作。<br>⑤ 修正 `:15` 的注释（与代码不符，会误导后续维护者）。 | `UserStore.cs:74-87`：`catch (Exception ex) { Log.Warning(ex, "users.json 解析失败，备份并初始化默认账号"); ... _users = CreateDefaultUsers(); Save(); }`。<br>`UserStore.cs:28-29`：`public const string DefaultAdminPassword = "gly"; public const string DefaultEngineerPassword = "gcs";`。<br>`UserStore.cs:308/317`：`PasswordHash = PasswordHasher.Hash(DefaultAdminPassword)` / `Hash(DefaultEngineerPassword)`。<br>`UserStore.cs:145-150`：`else if (string.IsNullOrEmpty(user.PasswordHash)) { result = FinalizeLogin(user); ... }`（免密放行）。 |
| **N-5** | 🟡 **P2** | concurrency | `MainAPP/ViewModels/AlarmCenterViewModel.cs:549-550, 554-555, 820-821, 848-849` | **`lock (_pendingDataSourceEvents)` 锁的是会被替换的字段本身（lock-on-mutable-field 反模式）。** 4 处临界区中，`:549-550` 与 `:820-821`/`:848-849` 在锁内**重新赋值 `_pendingDataSourceEvents`**（字段声明见 `:136`）。一旦 A 线程在锁内换掉了引用，B 线程随后进入 `lock (_pendingDataSourceEvents)` 锁的是**新对象**，与仍持有旧对象的 A 线程不再互斥，临界区保护被绕过。<br>并发是真实存在的：`RefreshActiveAlarms()` 的非阻塞路径在 `Task.Run` 后台线程执行（`:516-525`），`RefreshStats()` 同样（`:866 Task.Run(RefreshStatsCore)`），两者都会读写该字段；另有 3s 定时器（`:417`）与 60s 定时器（`:419`）分别驱动这两条路径（见 P2-7，定时器实际常驻不停）。<br>实际危害有限但真实：字段只被整体替换、List 内容在发布后不再原地修改，引用赋值本身原子，因此不会撕裂；但若将来有人改为原地 `Add/Remove`，保护将立即失效。属于「现在侥幸没事、改动即爆」的隐患。 | 增加专用锁对象 `private readonly object _pendingDataSourceLock = new();`，4 处统一 `lock (_pendingDataSourceLock)`，锁内不得再给 `_pendingDataSourceEvents` 以外的任何东西加锁或调用外部代码。 | `AlarmCenterViewModel.cs:136` `private List<AlarmEventRecord> _pendingDataSourceEvents = new();`<br>`:549-550` `lock (_pendingDataSourceEvents) { _pendingDataSourceEvents = pending; }`<br>`:554-555` `lock (_pendingDataSourceEvents) { return _pendingDataSourceEvents.ToList(); }`<br>`:820-821` / `:848-849` 同构。<br>并发来源：`:516` `Task.Run(() => { var pending = ResolvePendingDataSourceForActive(context); ... })`；`:866` `Task.Run(RefreshStatsCore).Forget();`。 |
| **N-6** | 🟡 **P2** | resource-leak | `Kanban.Collector/Services/EventBroadcaster.cs:112-118, 152-158`；`Kanban.Collector/Services/MetaPublisher.cs:212-216` | **`cancellationToken.Register(...)` 返回的 `CancellationTokenRegistration` 未保存、未 Dispose。** 三处订阅（报警流/状态流/元数据流）都用 `cancellationToken.Register(() => { lock (_gate) { _subscribers.Remove(channel); } })` 做退订，注册对象被丢弃。SignalR 的 `Context.ConnectionAborted` 是每个连接的独立 CTS，连接断开后 CTS 被释放，但已注册的回调对象若未被 Dispose，会在 CTS 存活期间持续占用其回调链表节点；高频重连（多屏端 + 网络抖动）下会累积。此外注册回调在锁内执行 `List.Remove`（O(n)），退订风暴时也会与 `Publish` 的扇出争抢 `_gate`。 | 保存 `CancellationTokenRegistration`（如放到返回的 `IAsyncEnumerable` 的 `finally` 中 Dispose，或挂在 channel 关联的上下文对象上），在读取循环结束时显式 Dispose；同时把 `_subscribers` 从 `List<T>` 换成 `HashSet<...>` 使退订降为 O(1)。 | `EventBroadcaster.cs:112-118`：<br>`cancellationToken.Register(() => { lock (_gate) { _alarmSubscribers.Remove(channel); } });`（返回值丢弃）<br>`MetaPublisher.cs:212-216` 同构（`_subscribers.Remove(channel)`）。<br>对比：`MetaPublisher.cs:65-67` 与 `:88-92` 的 `using var linkedCts` / `finally { _timer?.Dispose(); }` 说明该项目其它地方有正确的释放纪律，此处是遗漏。 |
| **N-7** | 🟡 **P2** | correctness / 弹性 | `MainAPP/Services/RemoteAuditService.cs:95, 118` | **Remote 审计查询同步阻塞 SignalR 调用，且没有任何超时保护。** 两处均为 `_monitorClient.QueryAuditLogsAsync(...).ConfigureAwait(false).GetAwaiter().GetResult()`——`ConfigureAwait(false)` 只影响 continuation 回跳，**不解除当前线程的阻塞**。对比同项目的 `RemoteHistoryQueryService` 已明确知道这个坑并做了规避：`RemoteHistoryQueryService.cs:529-547` 的注释写着「SignalR InvokeAsync 是真正异步的；直接 .GetAwaiter().GetResult() 在 UI 线程会死锁」，因此用 `Task.Run(...)` 包一层，并加 **60s 外层总闸（`:534`）+ 10s 分页闸（`:543-544`）**。`RemoteAuditService` 两者都没有。<br>影响：Collector 不可达/慢响应时，审计查询会**无限期占用线程池线程**（无 CTS），WPF 侧 UI 表现为转圈无响应且无法取消。当前调用方（`AuditQueryViewModel.cs:202/261/333` 均在 `Task.Run` 内）避免了 UI 死锁，但线程池饥饿与无超时问题依然存在；一旦有人从 UI 线程直接调用即触发死锁。 | 按 `RemoteHistoryQueryService.cs:527-571` 的既有模板改造：外层加 `using var overallCts = new CancellationTokenSource(TimeSpan.FromSeconds(60))`，内层每请求加 `CreateLinkedTokenSource` + `CancelAfter`，统一 `catch (OperationCanceledException)` 转本地化文案；同步阻塞仍保留但必须包在 `Task.Run` 内。 | `RemoteAuditService.cs:85-95`：<br>`var response = _monitorClient.QueryAuditLogsAsync(new AuditLogQueryRequest { ... }).ConfigureAwait(false).GetAwaiter().GetResult();`（无 CTS）<br>`RemoteAuditService.cs:108-118` 同构。<br>对照组：`RemoteHistoryQueryService.cs:534` `using var overallCts = new CancellationTokenSource(TimeSpan.FromSeconds(60));`；`:543-544` `using var pageCts = CancellationTokenSource.CreateLinkedTokenSource(overallCts.Token); pageCts.CancelAfter(RemoteCallTimeout);`；`:529-531` 注释。 |
| **N-8** | 🟢 **P3** | 异常吞没 / 可观测性 | `Kanban.Collector.Core/Services/ProductionHistoryWriter.cs:56-57`；`Kanban.Collector.Core/Services/HistoryStorageDiagnostics.cs:76` | **裸 `catch { }` 静默吞掉文件探测异常，诊断数据会「看起来正常地错」。** `ProductionHistoryWriter.GetDiagnosticsSnapshot()` 在 `try { if (File.Exists(_recoveryFilePath)) recoveryBytes = new FileInfo(_recoveryFilePath).Length; } catch { }`——文件被占用/权限不足时，`recoveryBytes` 静默为 0，运行监控页显示「恢复文件：不存在」，而磁盘上可能正躺着 200MB 未回放数据（`MaxRecoveryFileBytes` 上限见 `:17`），运维完全无感知。`HistoryStorageDiagnostics.Refresh():76` 的 `catch { }` 同理：WAL 文件大小探测失败时快照保持上一次的旧值，且不会有任何告警。 | 至少补 `Log.Debug(ex, ...)` 或 `Log.Warning(ex, ...)`；更稳妥的是引入显式的「诊断不可用」标志位（如 `recoveryBytes = -1`），让 UI 显示「未知」而不是「0 / 不存在」。 | `ProductionHistoryWriter.cs:55-57`：<br>`var recoveryBytes = 0L;`<br>`try { if (File.Exists(_recoveryFilePath)) recoveryBytes = new FileInfo(_recoveryFilePath).Length; }`<br>`catch { }`<br>`HistoryStorageDiagnostics.cs:76`：`catch { }`（紧接 `:74 lock (_syncRoot) _snapshot = snapshot;`）。 |

### 附加观察（未达缺陷级别，供决策参考）

1. **两条审计写入入口并存**：`KanbanAdminHub.RecordAuditAsync()`（`KanbanAdminHub.cs:35`）走注入的 `_auditService.Record`，而同文件其余 8 处（`SaveDevicesAsync/RollbackDevicesAsync/UpsertWorkOrderAsync/DeleteWorkOrderAsync/SaveCollectorSettingsAsync/SaveRecipesAsync/ApplyRecipeAsync`）走静态门面 `AuditLog.Record(...)`。经查 `AuditLog` 是同一 `IAuditService` 的静态包装（`Kanban.Collector.Core/Services/AuditLog.cs:14, 32-38, 52-62`），行为一致，**不是缺陷**，但双入口风格建议统一到注入式，便于测试替身。
2. **`RemoteRuntimeSink.ApplySnapshot` 的 O(n×m)**：`RemoteRuntimeSink.cs:376-385` 在 `foreach (var alarm in device.Alarms)` 内用 `snapshot.ActiveAlarms.First(a => a.AlarmId == alarm.Id)`（`:381`）做线性查找。设备报警数 × 活跃报警数的乘积，逐帧 500ms 执行。建议先 `ToDictionary` 建索引（`:375` 已经建了 `activeIds` HashSet，可复用为字典）。定 P3，未列入上表。
3. **`PlcBatchReadPlanner` 的 unchecked 算术**：`PlcBatchReadPlanner.cs:40` `start.Number + length * step` 无 `checked`；`:49/58` `(ushort)length` 强转。当前被 `length + slotsToAdd <= maxLength`（`:46`）约束（`maxLength` 由 `AppSettings.Validate` 限制为 ≤64），实际不可触发溢出。定 P3，未列入上表。

---

## 三、做得好的地方

1. **P0-1 的修复质量高**（`MainWindowViewModel.cs:484-509, 512-548, 565-588`）：没有简单地在 `Lazy` 工厂里加订阅了事，而是新增 `AttachPageViewModel` 显式入口 + `_attachedPageViewModels` HashSet 去重 + `Dispose` 统一遍历解绑，同时处理了「两条创建路径可能解析到同一实例」和「重复订阅」两个次生问题。这是本次复查中质量最高的一处改动。
2. **SignalR 通道设计扎实**（`EventBroadcaster.cs`、`MetaPublisher.cs`、`ConfigSyncHandler.cs`）：有界 Channel + `DropOldest` 防慢客户端 OOM；「补发 + 注册同一临界区」消除丢事件窗口（`EventBroadcaster.cs:102-110`，注释写明了原因）；`Publish` 的 seq 分配 + ring 写入 + 扇出单临界区（`:45-61`）。这些并发细节考虑得很到位。
3. **无 SQL 注入**：全解决方案唯一的原生 SQL（`DefectHistoryStore.cs:59-95, 110-137`）全部用 `FromSqlRaw` + `{0}/{1}/{2}` 参数占位符，无字符串拼接；其余查询均走 EF LINQ。生产代码中未发现 `ExecuteSqlRaw` 或插值 SQL。
4. **输入校验补齐到位**（`ConfigSyncHandler.cs:59-89, 176-183, 200-203`）：设备 Id/Name 非空与唯一性、TargetCycle > 0、枚举值 `EnsureEnumDefined`、工单 Id 范围、地址与数据源校验——注释明确标注「审查修复 2026-08-13」，说明上轮意见被认真采纳了。
5. **采集设置的事务式更新**（`ConfigSyncHandler.cs:300-399`）：草稿实例 → 完整 `Validate()` → 班次 1440 分钟全覆盖校验（`:312-320`）→ 先落盘后生效（`:328` vs `:334-341`）→ 失败时运行实例与 PLC 驱动保持原状。这个「先持久化后应用」的顺序在崩溃场景下是正确的。
6. **数据库迁移的防御**（`DatabaseProvider.cs:100-208`）：迁移前自动备份 + `PruneOldBackups` 只保留最近 3 份 + `Migrate()` 后再补一次 legacy patch + 迁移后校验关键表存在（`:145-147`）。崩溃重启循环不会撑爆磁盘。
7. **分页下推彻底**（`HistoryQueryHandler.cs` + `ProductionHistoryStore.cs` + `HistoryQueryLimits.cs`）：从「全量 ToList 经 SignalR 传输百万行」改为服务端 `Count + OrderBy + Skip/Take`，并用 `MaxFetchAllPages × MaxPageSize = 10 万` 封顶防深分页 DoS，跨进程契约常量集中在一处（`Kanban.Contracts/HistoryQueryLimits.cs`）。
8. **`async void` 基本清零**：全解决方案仅剩 WPF 框架强制的 `App.OnStartup`，且已配全局兜底。
9. **连接状态机与 UI 封送正确**（`MainWindowViewModel.cs:600-616, 654-699`）：`OnConnectionManagerPropertyChanged` 用 `dispatcher.CheckAccess()` 判断后 `InvokeAsync`；`OnConnectionStateChanged` 明确注释「后台采集线程触发，必须用 UI 线程封送」并全程 `_dispatcher.Invoke`。跨线程 UI 更新这块纪律良好。
10. **`DeviceRuntime` 的职责隔离**（`Models/DeviceRuntime.cs`）：配置与运行时分离，`UpdateFromCollector()` 作为 Remote 模式的唯一写入口（`:118-132`），OEE 公式单点收敛到 `OeeCalculator`（`:71-81` 注释明确禁止内联重复公式）。

---

## 四、审查覆盖说明与局限

### 覆盖到的
- **上轮 29 项**（1 P0 + 2 P1 + 14 P2 + 12 P3）逐条回源码核实，给出当前状态的判定与证据。
- **模式化搜索**：空 `catch {}`（全库）、`new Timer/DispatcherTimer`（全库 13 处逐一核对生命周期）、`Dictionary<>` 跨线程（重点看 `DataSourceAlarmTracker` / `AlarmStateTracker` / `UserStore` / `DashboardState`）、`lock` 可变字段、`FromSqlRaw`/`ExecuteSqlRaw`/插值 SQL、硬编码口令密钥、`GetAwaiter().GetResult()`/`async void`、事件 `+=`/`-=` 配对。
- **重点链路**：采集（PLC 轮询 → 状态机 → 入库 → SignalR 扇出 → 客户端灌 Runtime）、配置同步（MainAPP → Hub → ConfigSyncHandler → 落盘）、授权（LicenseGate / UserStore / PasswordHasher）、WPF 导航与页面生命周期。

### 未覆盖 / 局限（明确声明）
1. **未做动态验证**：全部结论来自静态代码阅读，未编译、未运行、未用调试器复现。N-1 的三条链路是按代码路径推演的，建议 Tessa 补 2 个回归测试坐实（① `recipes.json` 含 `D99999999999999` 时配方库是否被清空；② 单台设备配 `M99999999999999` 时全厂 `ScanAlarms` 是否持续返回 false）。
2. **未逐一核对的同类项**：`AlarmHistoryStore` / `StatusTransitionStore` 的 `OrderByDescending` 缺次级排序键（P2-12 的同类问题）是否也存在，未验证；`Kanban.Web` 的 `DashboardState` 之外是否有其它字典漏清 tombstone，未验证。
3. **`PlcSimulator` 覆盖较浅**：仅做了模式扫描（大量 `try { ... } catch { }` 属仿真器 CLI 的合理写法，如 `Program.cs:405-407/928/1142-1148/1201-1208` 的清理路径），未逐行审查其 TCP 中继实现。
4. **`Kanban.Analysis` 覆盖较浅**：项目文件数少，仅做了空 catch 与 SQL 拼接扫描，无命中。
5. **`LicenseIssuer.Wpf` 覆盖较浅**：确认了无吊销能力（P2-5 不再适用）与 `IssuerStore.Save()` 的落盘路径，未审查其签名密钥管理。
6. **UI 线程安全未能静态穷举**：WPF `ObservableCollection` 的跨线程修改只能逐个调用点判断。抽查了 `AlarmCenterViewModel.OnDevicesCollectionChanged()`（`:481` 正确用 `_uiDispatcher.BeginInvoke`）与 `RemoteRuntimeSink`（用 `ConcurrentDictionary`），未发现违规，但不能排除遗漏。
7. **N-2 的量化影响未测**：`SharedPlcDriverRouter` 持锁执行 IO 的实际阻塞时长取决于现场 PLC 型号与网络，未做实测；定级为 P1 是基于「持锁做阻塞 IO」这一确定性反模式 + 轮询与手动操作必然争抢的确定性，而非实测吞吐数据。
8. **N-4 的利用前提**：需要本地写 `users.json` 的权限，属本地提权/安全降级而非远程漏洞。若部署环境的数据目录已做严格 ACL，实际风险相应降低——但「管理员改密后仍可被重置回默认口令」这一点在任何部署下都成立。

### 给主理人的排期建议
- **立即（阻塞发布）**：**N-1**（根因一处改 `int.TryParse` 即可收敛三条链路，改动极小、风险极低，但爆炸半径最大）。
- **本迭代**：**N-4**（安全边界）、**N-3 + P2-7**（同一修复动作，页面生命周期接口化）、**P1-1**（加锁，10 行代码）。
- **下迭代**：**N-2**（驱动路由锁分离，需谨慎回归 PLC 通信）、**P2-1 / P2-2 / P2-6 / P2-8 / P2-9 / P2-10 / N-5 / N-7**。
- **backlog**：P2 剩余、P3 全部（含 N-6 / N-8）。
