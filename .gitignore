# ── .NET / 构建产物 ──
bin/
obj/
*.user
*.suo
*.cache
*.pdb
*.dll
*.exe
*.deps.json
*.runtimeconfig.json

# ── 测试输出 ──
TestResults/
*.trx
*.coverage
coverage_*.json

# ── IDE / 工具 ──
.vs/
.idea/
*.swp
*.sln.DotSettings.user

# ── 运行日志 / 本地生成物 ──
*.log
modbus_out*.txt
modbus_test_out*.txt
calib_run.log
screenshots/
CalibResults/

# ── 发布产物与本地演示运行目录（勿入库） ──
publish/
run-demo/

# ── 前端依赖（wasm-smoke.js 用 playwright-core，node_modules 勿入库） ──
node_modules/

# ── 中间/输出重定向目录（*.tmpbin* 含历史误配置的
#    "SourceCodeKanban0731Kanban.tmpbin 2Debug" 类目录） ──
*tmpbin*

# ── 工具数据（编辑器/工作台） ──
.arts/

# ── 运行时数据（本地配置/数据库，勿入库） ──
Config/
*.db
*.db-shm
*.db-wal

# ── 工作台工具数据 ──
.workbuddy/

# ── AI 代理运行时数据（会话事件流/快照/任务锁，勿入库） ──
.reasonix/
.zcode/
.codeartsdoer/temp/

# ── 代理记忆/工作笔记（本地工作区产物，勿入库） ──
memory/

# ── 授权签发产物（明文激活码/撤销记录，含密钥敏感性，严禁入库） ──
issued/
revoked/
LicenseIssuer.CLI/issued/

# ── GUI 审查产物（勿入库） ──
gui-test-screenshots/

# ── 预置暂存（勿入库） ──
.staging/
